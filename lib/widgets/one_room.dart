import 'package:flutter/material.dart';

class OneRoom extends StatelessWidget {
  const OneRoom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
